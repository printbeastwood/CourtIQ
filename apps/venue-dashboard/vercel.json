{
  "buildCommand": "cd ../.. && npx turbo run build --filter=@courtiq/venue-dashboard",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
